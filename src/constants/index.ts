export const TOTAL_REWARD_POOL = Number(
  import.meta.env.VITE_TOTAL_REWARD_POOL ?? 100_000
);

export const API_BASE_URL = 'https://api.prophecy.social/api/v1';

export const LEADERBOARD_INITIAL_DISPLAY = 10;
export const LEADERBOARD_MAX = 200;
